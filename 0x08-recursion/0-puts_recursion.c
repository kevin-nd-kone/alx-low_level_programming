#include "main.h"

/**
*_puts_recursion - print a string
*
*@s - char param
*
*/

_puts_recursion(char *s)
{
int i = sizeof s / sizeof s[i];
for(i = 0; i < i; i++)
{
_putchar(s[i]);
}
_putchar('\n');
}
