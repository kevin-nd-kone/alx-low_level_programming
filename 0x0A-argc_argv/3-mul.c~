#include <stdlib.h>
#include <stdio.h>

int main(int argc, char *argv[])
{
if(!argc > 3)
{
char error[] = "Error";
printf("%s\n",error);
return (1);
}
int i,res;
res = atoi(argv[1]) * atoi(argv[2]);
printf("%d\n", res);
return (0);
}
